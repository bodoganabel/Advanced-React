describe('Same test 101', () => {
    it('works as expected', () => {
        expect(1).toEqual(1);
    });
    it('adds two numbers together', () => {
        const age = 100;
        expect(age).toEqual(100);
    })
});